{
  "name": "norskvenn",
  "version": "0.5.0",
  "description": "Chrome extension that generates bilingual Norwegian + English subtitles for any YouTube video, all in your browser.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "vite": "^5.4.0"
  }
}
